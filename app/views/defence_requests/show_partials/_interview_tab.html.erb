<% if @policy.interview_start_time_edit? %>
  <div class="edit-time-of-arrival">
    <%= form_for(@defence_request_form, url: interview_start_time_defence_request_path) do |f| %>
      <%= render('shared/date_time_form_part', attribute_name: :interview_start_time, f: f) %>
      <%= hidden_field_tag :defence_request_id, @defence_request_form.defence_request.id %>
      <%= f.submit(t('add_interview_start_time'), class: 'button') %>
    <% end %>
  </div>
<% else %>
  <dl class="labels">
    <%= interview_start_time @defence_request %>
  </dl>
<% end %>

<% if @defence_request.solicitor_time_of_arrival? %>
  <dl class="labels">
    <%= display_value "solicitor_time_of_arrival", date_and_time_formatter(@defence_request.solicitor_time_of_arrival), id: "solicitor_time_of_arrival" %>
  </dl>
<% end %>

<% if @policy.solicitor_time_of_arrival_from_show? %>
  <p><%= link_to t('add_expected_time_of_arrival'), edit_defence_request_solicitor_arrival_time_path(@defence_request), class: 'button' %></p>
<% end %>

<div class="location">
  <h2>Location</h2>
  <p>This is a made up address because it's not implemented <%= link_to "XX9 9XX", "X" %></p>
</div>

<dl class="labels contacts">
  <h2>Contacts</h2>

  <dt>Custody officer</dt> <dd>Made up name</dd>
  <dt>Custody suite contact number</dt> <dd> <tel>000 0000 0000</tel> </dd>

  <%= display_value "investigating_officer_name", @defence_request.investigating_officer_name %>
  <%= display_value "investigating_officer_shoulder_number", @defence_request.investigating_officer_shoulder_number %>
  <%= display_value "investigating_officer_contact_number", @defence_request.investigating_officer_contact_number %>
</dl>
