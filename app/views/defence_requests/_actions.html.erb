<%= button_to 'Close', close_defence_request_path(def_req), method: :put if policy(def_req).close? %>
<%= link_to 'Edit', edit_defence_request_path(def_req) if policy(def_req).edit? %>
<%= button_to 'Open', open_defence_request_path(def_req), method: :put if policy(def_req).open? %>
