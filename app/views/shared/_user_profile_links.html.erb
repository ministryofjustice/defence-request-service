<% if current_user %>
  <% if Rails.env.development? %>
    <a class="user-links"><%= current_user.roles.first.humanize.upcase %></a>
  <% end %>
  <%= link_to t("sign_out"), Settings.authentication.site_url+"/users/sign_out", method: 'delete', class: 'user-links' %>
<% end %>
